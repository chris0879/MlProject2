name: my_ml_project
conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset_file: {type: string, default: './dataset.csv'}
    command: "python my_script.py --dataset $dataset_file"